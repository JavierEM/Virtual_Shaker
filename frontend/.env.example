VITE_API_BASE_URL=https://api.virtualshaker.com/v1
VITE_WS_BASE_URL=wss://realtime.virtualshaker.com/ws
# Set to true to force the frontend to use local in-memory mocks instead of calling the API
VITE_USE_MOCK_BACKEND=false
